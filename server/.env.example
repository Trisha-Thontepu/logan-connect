PGHOST=localhost
PGPORT=5432
PGUSER=loganconnect
PGPASSWORD=loganconnect_dev
PGDATABASE=logan_connect
PORT=4000

# Origin(s) allowed to call the API from a browser, comma-separated
CORS_ORIGIN=http://localhost:5173
# Public URL of the frontend, used in links sent to owners
PUBLIC_APP_URL=http://localhost:5173

# Real SMS (optional; the website demo does not need these)
TWILIO_AUTH_TOKEN=
TWILIO_WEBHOOK_URL=
